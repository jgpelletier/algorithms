/* create a list node structure to store ints.  */

int main ()
{
    /* create a linked-list with a head node and one element numbered 5. mallock
     * teh head node and the one data node. */

    /* print the value of every node in the list. one node at a time. one node
     * on a line. */

    /* free all nodes. */
    return 0;
}
