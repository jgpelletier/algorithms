struct _line_t {
    char line[120];
    struct _line_t*  right;
    struct _line_t*  left;
};

typedef struct _line_t lines;

void print_city

// What signitures do I need in here?
// maxValue
// minValue
// search
// insertion
// deletion
