#include <stdio.h>

/*This is a multi line comment. It will fit as many characters as needed*/
int main(int argc, char *argv[])
{
    int distance = 100;

    //this is another comment but it only goes to the end of the line.
    printf("You are %d miles away.\n", distance);

    return 0;
}
